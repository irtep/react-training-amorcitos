import { mainHeaders } from '../arrays/arrays.js';

// calls random dice, for example 6 is 1d6
export function callDice(max){
    const result =  1 + Math.floor(Math.random() * max);
    return result;
} 

// makes link list
export function makeLinkMenu(targetMenu) {


/* refs:

    name: "Entertainment",
    links: [
      { name: "Diario", url: "http://irtep.net/diario" },

  targetState.map(selectedMenu => {
            return (                      
              <LinksItem
                key ={selectedMenu.id}
                title ={selectedMenu}
                handleClick ={this.clickControl}/>
            )
          })
*/ 
}
